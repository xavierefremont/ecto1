
    // 1 0
    if(!feof(stdin)){
        readDatas();
        v.x = 1;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //1 1
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = 1;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //2 1
    if(!feof(stdin)){
        readDatas();
        v.x = 1;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //3 1
    if(!feof(stdin)){
        readDatas();
        v.x = 1;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //3 0
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = -1;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //3 0
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //3 0
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //3 1
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = 1;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //3 1
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //3 1
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //3 2
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = 1;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //2 2
    if(!feof(stdin)){
        readDatas();
        v.x = -1;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //1 2
    if(!feof(stdin)){
        readDatas();
        v.x = -1;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //0 2
    if(!feof(stdin)){
        readDatas();
        v.x = -1;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //-1 2
    if(!feof(stdin)){
        readDatas();
        v.x = -1;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //-2 2
    if(!feof(stdin)){
        readDatas();
        v.x = -1;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //-3 1
    if(!feof(stdin)){
        readDatas();
        v.x = -1;
        v.y = -1;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //-3 0
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = -1;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //-3 0
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //-3 0
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //-3 0
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //-3 0
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //-3 0
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //-3 0
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //-3 0
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //-3 0
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //-3 0
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //-2 -1
    if(!feof(stdin)){
        readDatas();
        v.x = 1;
        v.y = -1;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //-1 -1
    if(!feof(stdin)){
        readDatas();
        v.x = 1;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //-1 -2
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = -1;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //-0 -2
    if(!feof(stdin)){
        readDatas();
        v.x = 1;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }


    //1 -2
    if(!feof(stdin)){
        readDatas();
        v.x = 1;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //2 -2
    if(!feof(stdin)){
        readDatas();
        v.x = 1;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //3 -1
    if(!feof(stdin)){
        readDatas();
        v.x = 1;
        v.y = 1;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //3 0
    if(!feof(stdin)){
        readDatas();
        v.x = 1;
        v.y = 1;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }

    //3 0
    if(!feof(stdin)){
        readDatas();
        v.x = 0;
        v.y = 0;
        sendDatas(v);
    }else{
        return EXIT_SUCCESS;
    }