map : tableau dynamique avec la taille de la map T1
    struct Coordonnées {
    int x
    int y

    }
    struct map {
    	   coordonées c;
	   char** plan;
	   }
	   
      1.1 creation avec malloc createMap(x, y) return struct map

      2.1 lecture map entière initMap(struct map*)

      3.1 recupère coordonnées setCoordonnées(Coordonnées a, b, c) 
      3.3 envoie map au gps // ou liste sendToGPS(strucs map (ou liste))
